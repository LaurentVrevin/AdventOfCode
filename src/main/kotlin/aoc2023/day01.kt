package aoc2023

import utils.readInput

fun main() {
    val inputs = readInput("2023", "day01")
    //check if file acces is ok, and algo in Aoc2023Utils.kt works
    inputs.forEach {line ->
        println(line)
    }
}
